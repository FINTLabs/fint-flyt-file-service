fint:
  org-id: fintlabs.no
  flyt:
    resource-server:
      security:
        api:
          internal-client:
            enabled: true
            authorized-client-ids:
  kafka:
    default-replicas: 1
spring:
  kafka:
    bootstrap-servers: localhost:9092
server:
  port: 8091
